starting_balance: 10000.0
max_trades_per_day: 20
max_consecutive_losses: 3
max_daily_loss_amount: 120.0
max_daily_loss_percent: 0.015
max_daily_profit_target: 250.0
loss_cooldown_minutes: 15
consecutive_loss_cooldown_minutes: 60
consecutive_loss_cooldown_trigger: 2
max_simultaneous_positions: 1
min_confidence: 0.70
allowed_sessions:
  - london
  - new_york
allowed_symbols:
  - EURUSD
allowed_timeframes:
  - 1m
enabled_rules:
  - max_trades_per_day
  - max_consecutive_losses
  - max_daily_loss_amount
  - max_daily_loss_percent
  - max_daily_profit_target
  - cooldown_after_loss
  - max_simultaneous_positions
  - minimum_confidence
  - session_restriction
  - symbol_whitelist
  - timeframe_whitelist
